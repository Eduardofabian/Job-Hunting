# Optional — only needed if you set USE_GOOGLE_API = True in config.py.
# By default job-hunting uses DuckDuckGo and needs no keys.
GOOGLE_API_KEY=your_google_api_key_here
GOOGLE_CSE_ID=your_custom_search_engine_id_here
